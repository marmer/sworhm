version: '3.3'

services:
  db:
    image: postgres:10-alpine
    environment:
      POSTGRES_USER: sworhm
      POSTGRES_PASSWORD: sworhmpws
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - sworhm-db-volume:/var/lib/postgresql/data/pgdata
    ports:
      - 5432:5432

volumes:
  sworhm-db-volume:
