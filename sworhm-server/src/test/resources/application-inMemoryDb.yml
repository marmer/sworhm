logging:
  level:
    - logging.level.org.springframework.web: info
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database: h2
  datasource:
    url: jdbc:h2:mem:sworhm
