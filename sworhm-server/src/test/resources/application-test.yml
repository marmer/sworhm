logging:
  level:
    - logging.level.org.springframework.web: info
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:h2:mem:sworhm
    username: sworhm
    password: