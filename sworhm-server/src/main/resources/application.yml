logging:
  level:
    - logging.level.org.springframework.web: warn
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    database: postgresql
  datasource:
    url: jdbc:postgresql://localhost:@testDbPort@/sworhm
    username: sworhm
    password: sworhmpw
  test:
    database:
      replace: none